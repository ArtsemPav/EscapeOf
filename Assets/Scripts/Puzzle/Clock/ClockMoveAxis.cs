/// <summary>
/// The local-space axis along which the clock moves when the puzzle is solved.
/// </summary>
public enum ClockMoveAxis
{
    X,
    Y,
    Z,
    /// <summary>Uses the custom direction vector defined on the controller.</summary>
    Custom
}
